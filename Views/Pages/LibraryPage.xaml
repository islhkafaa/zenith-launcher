<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Zenith_Launcher.Views.Pages.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Zenith_Launcher.Views.Controls"
    xmlns:models="using:Zenith_Launcher.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock
                Text="Library"
                Style="{StaticResource TitleTextBlockStyle}"
                VerticalAlignment="Center"/>
            <Button

                Click="OnAddGameClick"
                HorizontalAlignment="Right"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Add Game"/>
                </StackPanel>
            </Button>
        </Grid>

        <ProgressRing
            Grid.Row="1"
            IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
            Width="48"
            Height="48"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>

        <GridView
            x:Name="GamesGridView"
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.FilteredGames, Mode=OneWay}"
            SelectionMode="None"
            IsItemClickEnabled="False">

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="6"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Game">
                    <controls:GameCard
                        Game="{x:Bind}"
                        Margin="8"
                        LaunchRequested="OnGameLaunchRequested"
                        DeleteRequested="OnGameDeleteRequested"
                        EditRequested="OnGameEditRequested"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <StackPanel
            x:Name="EmptyStatePanel"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="24"
            Visibility="Collapsed">

            <FontIcon
                Glyph="&#xE7FC;"
                FontSize="96"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>

            <StackPanel Spacing="8" HorizontalAlignment="Center">
                <TextBlock
                    Text="No games in your library"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    HorizontalAlignment="Center"/>
                <TextBlock
                    Text="Add games manually to get started"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Center"/>
            </StackPanel>

            <Button
                Click="OnAddGameClick"
                HorizontalAlignment="Center"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Add Your First Game"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
