<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="PageRoot"
    x:Class="Zenith_Launcher.Views.Pages.StoresPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Zenith_Launcher.ViewModels"
    xmlns:local="using:Zenith_Launcher.Views.Pages"
    mc:Ignorable="d">

    <Page.Resources>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock
                    Text="Store Integrations"
                    Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock
                    Text="Scan and import your game libraries from Steam, Epic Games, and GOG"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Margin="0,8,0,0"/>
            </StackPanel>
            <Button
                Content="Scan All Stores"
                Command="{x:Bind ViewModel.ScanAllStoresCommand}"
                HorizontalAlignment="Right"
                Style="{StaticResource AccentButtonStyle}"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,0,0,16" Visibility="{x:Bind ViewModel.IsScanning, Mode=OneWay}">
            <ProgressBar IsIndeterminate="True" Margin="0,0,0,8"/>
            <TextBlock
                Text="{x:Bind ViewModel.ScanStatus, Mode=OneWay}"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <ItemsControl
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.Stores, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:StoreInfo">
                    <Grid
                        Style="{StaticResource GameCardStyle}"
                        Padding="24"
                        Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border
                            Grid.Column="0"
                            Width="64"
                            Height="64"
                            CornerRadius="8"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            Margin="0,0,24,0">
                            <FontIcon
                                Glyph="&#xE719;"
                                FontSize="32"
                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="8">
                            <TextBlock
                                Text="{x:Bind Name}"
                                Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Border
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Visibility="{x:Bind IsInstalled, Mode=OneWay}">
                                    <TextBlock
                                        Text="Installed"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                                </Border>
                                <Border
                                    Background="{ThemeResource SystemFillColorCriticalBrush}"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Visibility="{x:Bind IsInstalled, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                    <TextBlock
                                        Text="Not Installed"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                                </Border>
                            </StackPanel>
                            <TextBlock
                                Style="{StaticResource BodyTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Visibility="{x:Bind GamesFound, Mode=OneWay, Converter={StaticResource IntToVisibilityConverter}}">
                                <Run Text="{x:Bind GamesFound, Mode=OneWay}"/>
                                <Run Text="games found"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Spacing="8" VerticalAlignment="Center">
                            <Button
                                Content="Scan Library"
                                Command="{Binding DataContext.ScanStoreCommand, ElementName=PageRoot}"
                                CommandParameter="{x:Bind Name}"
                                IsEnabled="{x:Bind IsInstalled, Mode=OneWay}"/>
                            <Button
                                Content="Import Games"
                                Command="{Binding DataContext.ImportGamesCommand, ElementName=PageRoot}"
                                CommandParameter="{x:Bind Name}"
                                Style="{StaticResource AccentButtonStyle}"
                                Visibility="{x:Bind GamesFound, Mode=OneWay, Converter={StaticResource IntToVisibilityConverter}}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
